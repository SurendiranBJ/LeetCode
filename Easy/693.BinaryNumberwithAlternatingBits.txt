class Solution(object):
    def hasAlternatingBits(self, n):
        """
        :type n: int
        :rtype: bool
        """
        b=format(n,'b')
        for i in range(1,len(b)):
            if b[i-1]!=b[i]:
                continue
            else:
                return False
        return True            